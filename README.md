# retail_website
“I implemented REST APIs for authentication and order management using Spring Boot. I used Spring Data JPA repositories for persistence, leveraging built-in methods and custom queries . The service layer handled business logic such as credential validation and order total calculation before persisting data.
